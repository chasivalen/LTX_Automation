{
  "name": "Reflex Build Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.9", // Use a Python base image
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "customizations": {
    "vscode": {
      "settings": {
        "python.pythonPath": "/usr/local/bin/python3"
      },
      "extensions": [
        "ms-python.python", // Python support
        "ms-python.vscode-pylance", // Pylance for Python
        "esbenp.prettier-vscode" // Prettier for formatting
      ]
    }
  },
  "postCreateCommand": "pip install -r requirements.txt", // Install dependencies from requirements.txt
  "remoteUser": "vscode" // Run as the VS Code user
}
